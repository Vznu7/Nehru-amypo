#include <stdio.h>
int main()
{
    float a;
    float radians(float d)
    {
        float b = a*(3.14/180);
        return b;
        
    }
    scanf("%f",&a);
    float r = radians(a);
    printf("%.2f",r);
    
}